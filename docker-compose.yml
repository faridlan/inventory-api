version: '3.8'

services:
  inventory-api:
    build: .
    container_name: inventory-api-container
    ports:
      - "8080:8080"

    env_file:
      - .env
      
    networks:
      - database_luar

networks:
  database_luar:
    external: true
    name: psqldb_network